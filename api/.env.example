# ===========================================
# GO BACKEND ENVIRONMENT CONFIGURATION
# ===========================================

# LLM Configuration
# Choose your LLM provider: 'ollama' or 'bedrock'
LLM_PROVIDER=ollama
# Model to use (depends on provider)
LLM_MODEL=qwen2.5:1.5b

# AWS Bedrock Configuration (only needed if using Bedrock)
# Your AWS region (eu-central-1, us-east-1, etc.)
AWS_REGION=eu-central-1
# Your AWS access key ID
AWS_ACCESS_KEY_ID=your-access-key-here
# Your AWS secret access key
AWS_SECRET_ACCESS_KEY=your-secret-key-here

# Ollama Configuration (only needed if using Ollama)
# Ollama server URL
OLLAMA_HOST=http://localhost:11434

# Server Configuration
# Port for the Go backend server
SERVER_PORT=8084
# Default directory for created projects
PROJECT_PATH=./projects
# Default project name
DEFAULT_PROJECT_NAME=my-react-app

# Available Models Configuration
# Comma-separated list of available Ollama models
OLLAMA_MODELS=qwen2.5:1.5b,cogito:8b,gemma3:1b,qwen3:0.6b
# Comma-separated list of available Bedrock models
BEDROCK_MODELS=anthropic.claude-3-5-sonnet-20241022-v2:0,anthropic.claude-3-haiku-20240307-v1:0,anthropic.claude-3-opus-20240229-v1:0

# API Configuration (for documentation, used by frontend)
API_BASE_URL=http://localhost:8084/api
WEBSOCKET_URL=ws://localhost:8084/api/v1/chat/stream
